import React from 'react'
import {LoginReduxForm} from "../loginForm/LoginForm";

export const LoginPage = () => {

    return (
        <div>
            <h1>Login</h1>
            <LoginReduxForm />
        </div>

    )
}