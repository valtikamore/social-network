import {followAc, userType} from "./users-reducer";

describe('usersReducer follow should be',()=> {
    test('...',()=> {
        const startValue = {
            users: [

            ] as userType[]
        }
        const action = followAc(1)
    })
})